# Учебники (копирайт + большой размер) — не в репозиторий
materials/*.pdf

# Системное / временное
.DS_Store
/tmp/
*.log

# учебники в корне — копирайт, не публиковать
/*.pdf

# секрет — ключ OpenAI (не коммитить)
~:.deutsch_openai_key/
*.pages
.deutsch_openai_key*
